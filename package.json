{
  "name": "arcade-router",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static client ready')\"",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
