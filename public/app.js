const form = document.querySelector('#join-form'), message = document.querySelector('#message');
form.addEventListener('submit', async event => { event.preventDefault(); const button = form.querySelector('button'), payload = Object.fromEntries(new FormData(form)); button.disabled = true; button.textContent = 'CONNECTING…'; message.textContent = 'Checking encrypted room credentials…'; try { const response = await fetch('/api/room', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'join', pin: payload.room.replace(/\s/g, ''), player: payload.player }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Could not join this room'); sessionStorage.setItem('expedition-session', JSON.stringify({ room: data.room.pin, player: payload.player })); window.location.assign('/lobby.html'); } catch (error) { message.textContent = error.message; button.disabled = false; button.innerHTML = "Connect to mission <b>↗</b>"; } });
